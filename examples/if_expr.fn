
run = fn () {
    Num = random.uniform(2)

    if Num === 1 {
        # without parens
        io.format("is one~n")
    } else {
        io.format("not one~n")
    }

    if (Num === 1) {
        # with parens
        io.format("is one~n")
    } else if Num === 2 {
        io.format("is two~n")
    } else {
        io.format("not one not two~n")
    }

    if Num === 99 {
        io.format("is 99~n")
    } else {
        io.format("not 99~n")
    }

}
