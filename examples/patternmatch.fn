fun = fn ((_ T, (N _))=A) {
    io.format("~p ~p ~p~n" [T N A])
}

test = fn (('+' = Op Num)) {
    io.format("~p ~p~n", [Op Num])
}

public run = fn () {
    A = (complex tuple, (1 true))
    (_ T, (N _)) = A

    io.format("~p ~p~n" [T N])
    fun(A)
    test(('+' 1))
}
