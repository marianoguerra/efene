
run = fn () {
    A = <[1]>;
    B = <[1, 2, 3, 4]>;
    C = <[1:8 unsigned little integer, 2:4, 3:2, 4]>;
    D = <["mariano"]>;

    io:format("~p~n", [A]);
    io:format("~p~n", [B]);
    io:format("~p~n", [C]);
    io:format("~p~n", [D]);
};
