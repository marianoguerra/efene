
loop = fn () {
    receive (Stuff) {
        io.format("~p~n" [Stuff])
    }
    loop()
}

public run = fn () {
    Pid = spawn(fn loop/0)
    Pid1 = spawn(fn loop/0)
    Pid ! Pid1 ! 1
    Pid ! Pid1 ! atom
    Pid ! Pid1 ! "string"
    ok
}
