foo = fn ((a _ _) = T, stuff = A) {
    io.format("starts with a: ~p ~p~n" [T A])
} ((b _ _) = T, hi = A) {
    io.format("starts with b: ~p ~p~n" [T A])
}

public run = fn () {
    foo((a 1 false), stuff)
    foo((b 2 true), hi)
}
