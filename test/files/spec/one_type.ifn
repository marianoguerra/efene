# see the erlang file with the same name for description on what are we testing

@public
@spec(integer()) -> integer()
f_1 = fn (Int)
    Int

@public
@spec(tuple()) -> tuple()
f_2 = fn (Tuple)
    Tuple

@public
@spec(4) -> 5
f_3 = fn (4)
    5

@public
@spec(<[]>) -> <[]>
f_4 = fn (<[]>)
    <[]>

@public
@spec([]) -> []
f_5 = fn ([])
    []

@public
@spec((,)) -> (,)
f_6 = fn ((,))
    (,)

@public
@spec(list()) -> list()
f_7 = fn (List)
    List

@public
@spec(some_atom) -> 'if'
f_8 = fn (some_atom)
    'if'
