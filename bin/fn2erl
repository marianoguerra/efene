#!/usr/bin/env sh
SCRIPT=$(readlink -f $0)
# Absolute path this script is in.
SCRIPTPATH=`dirname $SCRIPT`
OLD=$(pwd)
cd $SCRIPTPATH
erl -run efene print_erlang $OLD/$1 -run init stop -noshell
cd $OLD
